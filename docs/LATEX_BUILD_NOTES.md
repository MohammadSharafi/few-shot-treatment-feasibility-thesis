# LaTeX Build Notes

Date: 2026-05-25  
Build target: Persian thesis `thesis/thesis_fa.pdf` and defense slides `thesis/defense_slides.pdf`.

## Build Engine

- Required engine: XeLaTeX / XePersian.
- Do not use pdfLaTeX.
- Main file: `thesis/thesis_fa.tex`.
- Bibliography: BibTeX pass is handled by `make thesis-fa`.

## Clean Behavior

`make clean-temp` is expected to remove auxiliary/build files while preserving final PDFs. Final PDFs are either preserved or regenerated by the following build targets:

- `thesis/thesis_fa.pdf`
- `thesis/defense_slides.pdf`

## Final Expected Command Sequence

```bash
make clean-temp
make thesis-fa
make validate
make defense
```

The project is ready only if the full sequence passes from the project root.

## Current Build Quality Targets

- Missing-character warnings: 0 expected.
- Undefined citations: 0 expected.
- Undefined references: 0 expected.
- PDF metadata title: must exactly match the official Persian thesis title.
- Canonical metrics: must remain unchanged.

## Typography Notes

- Persian body font/template was not randomly changed.
- English terms in Persian text use `\lr{...}`.
- Code and file identifiers use `\texttt{\lr{...}}`.
- Headings with English technical terms use safe PDF strings via `\texorpdfstring` where needed.
- Chapter 5 captions and table headers were polished for RTL/LTR safety.

## Remaining Non-Blocking Warnings

The latest final validation showed 37 overfull hbox warnings, all within cosmetic tolerance. Visual review did not identify a visible margin overflow that blocks defense/submission. The previously visible Table 7.3 overlap and the dense metric-header collisions in Chapter 5/appendix tables were corrected and rechecked on rendered PDF pages.

## Final Verified Sequence

The following sequence passed from the project root on 2026-05-25:

```bash
make clean-temp
make thesis-fa
make validate
make defense
```

Final validation reported 56 passed checks, 0 warnings, 0 failed checks, 0 missing-character warnings, 0 undefined citations, 0 undefined references, and 24 cosmetic overfull hbox warnings within tolerance.

## Status

Pass. The XePersian build and defense artifacts regenerate cleanly from a fresh temporary-file cleanup.
